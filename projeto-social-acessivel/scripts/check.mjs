import{access,readFile}from'node:fs/promises';const pages=['index.html','projetos.html','cadastro.html'];let failures=[];for(const page of pages){const html=await readFile(new URL(`../${page}`,import.meta.url),'utf8');for(const token of['lang="pt-BR"','<main id="conteudo">','class="skip-link"','aria-label="Navegação principal"'])if(!html.includes(token))failures.push(`${page}: ausência de ${token}`);for(const src of html.matchAll(/(?:src|href)="((?:css|js|img)\/[^\"]+)"/g)){try{await access(new URL(`../${src[1]}`,import.meta.url))}catch{failures.push(`${page}: arquivo não encontrado ${src[1]}`)}}}if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Verificações estruturais concluídas sem falhas.');

